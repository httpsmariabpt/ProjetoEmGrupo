@model IEnumerable<projeto.Models.Veterinario>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CRMV)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Celular)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Especialidade)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CRMV)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Celular)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Especialidade.EspecialidadeId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.VeterinarioId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.VeterinarioId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.VeterinarioId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
